import { SiteHeader } from "@/components/site-header";
import React from "react";

const CreatePayments = () => {
    return (
        <div className="flex flex-col">
            <SiteHeader title="Create Payment" />
        </div>
    );
};

export default CreatePayments;
